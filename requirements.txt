# One-button install: pip install -r requirements.txt

# Google Gemini LLM (free tier)
google-generativeai

# Database (Supabase - handles both file storage and Postgres)
supabase

# File extraction
pdfplumber          # PDF text extraction
python-docx         # DOCX text extraction

# API server
fastapi
uvicorn[standard]

# Structured LLM outputs
pydantic

# Utilities
python-dotenv       # Load .env config
httpx               # Async HTTP calls
